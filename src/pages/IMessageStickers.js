import React from "react";
import PagePosts from '../components/PagePosts.js';

const IMessageStickers = (props) => (
  <main>
    <PagePosts slug={ props.slug }/>
  </main>
);


export default IMessageStickers;